'use client';import {FormEvent,useState} from 'react';import {X} from 'lucide-react';export default function LeadModal({open,onClose,scene}:{open:boolean;onClose:()=>void;scene:string}){const[status,setStatus]=useState('');if(!open)return null;async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setStatus('Đang gửi...');const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/leads',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...data,scene})});setStatus(r.ok?'Đã nhận thông tin. Chúng tôi sẽ liên hệ sớm.':'Có lỗi xảy ra, vui lòng thử lại.')}return <div className="fixed inset-0 z-[100] grid place-items-center bg-black/70 p-4 backdrop-blur-md"><div className="w-full max-w-lg rounded-3xl border border-white/10 bg-zinc-950 p-7 shadow-2xl"><div className="mb-6 flex items-start justify-between"><div><p className="text-xs uppercase tracking-[.25em] text-amber-400">Private Viewing</p><h2 className="mt-2 text-3xl font-light">Đăng ký trải nghiệm</h2><p className="mt-2 text-sm text-white/50">Scene hiện tại: {scene}</p></div><button onClick={onClose}><X/></button></div><form onSubmit={submit} className="grid gap-3"><input required name="name" placeholder="Họ và tên" className="rounded-xl border border-white/10 bg-white/5 px-4 py-3 outline-none"/><input required name="phone" placeholder="Số điện thoại" className="rounded-xl border border-white/10 bg-white/5 px-4 py-3 outline-none"/><input name="email" type="email" placeholder="Email" className="rounded-xl border border-white/10 bg-white/5 px-4 py-3 outline-none"/><button className="mt-2 rounded-xl bg-amber-400 px-4 py-3 font-semibold text-black">Gửi đăng ký</button>{status&&<p className="text-sm text-white/60">{status}</p>}</form></div></div>}
